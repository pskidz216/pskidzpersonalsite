# Full-resolution masters

The 4K PNG originals for every image in this folder live **outside the vault**
(93 MB — too heavy for Obsidian to index):

    ~/Pictures/pristine-coastal-hero-masters/

The `.jpg` files here are the web-sized derivatives actually used by the pages
(2400px heroes, 1400px cases, 900px neighbourhood cards).

Includes `hero-2-street-REJECTED.jpg` — the first street frame, rejected as too
saturated and postcard-like against both palettes.
